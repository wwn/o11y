# Default port for o11y-app, but can be overridden by subprojects
quarkus.http.port=49255
quarkus.application.name=o11y-app
o11y.app.delay=2s
o11y.app.hops=10
o11y.app.initial-sequence.enabled=true
quarkus.rest-client.london-api.url=${LONDON_API_URL:http://127.0.0.1:49252}
quarkus.rest-client.firenze-api.url=${FIRENZE_API_URL:http://127.0.0.1:49253}
quarkus.rest-client.dresden-api.url=${DRESDEN_API_URL:http://127.0.0.1:49254}

# --- Logging ------------------------------------------------------------------
# Non-dev profiles (prod / container): structured ECS JSON on stdout. Filebeat
# ships it verbatim to Elasticsearch - no Logstash / grok parsing needed.
# The `dev` profile (quarkusDev from the IDE) and `test` fall back to a readable
# console pattern instead.
quarkus.log.level=INFO
quarkus.log.category."ch.nickl.o11y".level=DEBUG

quarkus.log.json.log-format=ecs
quarkus.log.json.fields.mdc.enabled=true
quarkus.log.json.fields.mdc.flat-fields=true
quarkus.log.json.additional-field."service.name".value=${quarkus.application.name}
quarkus.log.json.additional-field."service.version".value=${quarkus.application.version}

%dev.quarkus.log.json.console.enabled=false
%test.quarkus.log.json.console.enabled=false
quarkus.log.console.format=%d{HH:mm:ss.SSS} %-5p [%c{2.}] (%t) %X %s%e%n
# Darken the console colour scheme so it stays readable on a light terminal
# background (ignored in prod where the JSON formatter owns the console).
quarkus.log.console.darken=1
